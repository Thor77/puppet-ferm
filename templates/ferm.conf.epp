<%- | Hash $rules, Hash $rule_defaults | -%>
# Ferm configuration
#
# MANAGED BY PUPPET
<% $rules.each |$comment, $rule| { %>
# <%= $comment %>
<%= ferm::build_rule($rule, $rule_defaults, $comment) %>
<% } %>
